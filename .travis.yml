sudo: required
services:
    - docker
before_install:
    - docker build -t oleksandrkhrystenko/docker-react -f Dockerfile.dev .
script:
    -  docker run -e CI=true oleksandrkhrystenko/docker-react npm run test